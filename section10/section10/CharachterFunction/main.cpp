#include <iostream>
#include <cctype>           // you have to add this to be able to use charachter functions

using namespace std;

int main(){
        // to be able to use charachter funtions you have to include the cctype module in your code
        // you then have to use the         function_name (char)
        
        /*
         *      isalpha(c)       checks if it is a letter
         *      isalnum(c)       checks if it is a letter or digit
         *      isdigit(c)       checks if it is a digit
         *      islowe(c)        checks if it is lowercase
         *      isprint(c)       checks if it is a printable char
         *      ispunct(c)       true if c is a punctiation char
         *      isupper(c)       checks if it is a uppercase letter
         *      isspace(c)       checks if it is whitespace
         * 
         * 
         *      tolower(c)      returns lowercase of c
         *      toupper(c)      returns uppercase of c
         * 
         * 
         * 
         * */
        
        return 0;
}